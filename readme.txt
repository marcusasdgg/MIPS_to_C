this program will attempt to compile c like syntax code to mips 5 assembly code. It will support various keywords and 3 types so far
string int and double and char in the future.
for the while for and if statements it is crucial that indentation is added for teh brackets sucht thaat the brackets are on the same
level
i.e
for (...)
{

}
to use execute program in commandd line with cli arguments -> path of input (c or cpp file), path of output (.a file)
tbh any txt file will work for input and output.

./(program name) path_input path_output
-